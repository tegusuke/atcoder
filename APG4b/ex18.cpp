#include <bits/stdc++.h>
using namespace std;

int main() {
  int n, m;
  cin >> n >> m;
  vector<int> w(m), l(m);
  for (int i = 0; i < m; i++) {
    cin >> w.at(i) >> l.at(i);
  }
  vector<vector<int>> ans(n, vector<int>(n));
  for (int i = 0; i < n; i++) {
    for (int j = 0; j < n; j++) {
      
    }
  }
}